{
  "name": "datadog-workshop-starter",
  "version": "0.1.0",
  "description": "Starter project for Datadog workshop with TypeScript",
  "scripts": {
    "start:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npx serverless offline",
    "deploy:backend": "cd backend && npx serverless deploy",
    "deploy:frontend": "cd frontend && npm run build && aws s3 sync build/ s3://your-bucket-name"
  },
  "dependencies": {
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@types/node": "^18.15.3"
  }
}